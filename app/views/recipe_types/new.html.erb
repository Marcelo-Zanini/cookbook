<% @recipe_type.errors.full_messages.each do |error| %>
  <%= error %>
<% end %>

<%= form_for @recipe_type do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.submit t(:send)%>
<% end%>
